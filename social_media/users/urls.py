from django.urls import path
from . import views

app_name = 'users'

urlpatterns = [
    # Routes supplémentaires pour l'app users (si nécessaire dans le futur)
    # Les routes principales sont maintenant dans social_media/urls.py
]
